#include<stdio.h>
void main()
{
      int a, b;
      printf("Enter the beginning and end of the range : \n");
      scanf("%d%d",&a,&b);
      for(int i=a;i<=b;i++)
      {
           printf("%d",i);
      }
}
